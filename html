<!DOCTYPE html>
<html lang="zh-TW">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Peggy's Illustration</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Caveat:wght@400;700&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Helvetica Neue', Arial, sans-serif;
            line-height: 1.6;
            color: #2c2c2c;
            background: #ffffff;
        }

        /* 側邊欄 */
        .sidebar {
            position: fixed;
            left: 0;
            top: 0;
            width: 280px;
            height: 100vh;
            background: #ffffff;
            border-right: 1px solid #e8e8e8;
            padding: 3rem 2rem;
            z-index: 1000;
            display: flex;
            flex-direction: column;
        }

        .logo {
            font-family: 'Caveat', cursive;
            font-size: 2.2rem;
            font-weight: 700;
            color: #2c2c2c;
            text-decoration: none;
            margin-bottom: 4rem;
            display: block;
            transition: opacity 0.3s ease;
            cursor: pointer;
        }

        .logo:hover {
            opacity: 0.6;
        }

        .sidebar nav ul {
            list-style: none;
            display: flex;
            flex-direction: column;
            gap: 2rem;
        }

        .sidebar nav a {
            color: #2c2c2c;
            text-decoration: none;
            font-size: 0.9rem;
            font-weight: 400;
            letter-spacing: 0.5px;
            text-transform: uppercase;
            transition: opacity 0.3s ease;
            display: block;
            cursor: pointer;
        }

        .sidebar nav a:hover {
            opacity: 0.6;
        }

        .sidebar nav a.active {
            opacity: 0.5;
        }

        /* 主要內容區 */
        .main-content {
            margin-left: 280px;
            min-height: 100vh;
        }

        /* 頁面區塊 */
        .page {
            display: none;
            animation: fadeIn 0.5s ease;
        }

        .page.active {
            display: block;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
        }

        /* 區塊共用樣式 */
        section {
            padding: 6rem 4rem;
            max-width: 1400px;
        }

        section h2 {
            font-size: 1.2rem;
            text-align: center;
            margin-bottom: 4rem;
            color: #2c2c2c;
            font-weight: 400;
            letter-spacing: 2px;
            text-transform: uppercase;
        }

        /* 插畫作品網格 */
        .illustration-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 1rem;
        }

        .illustration-item {
            aspect-ratio: 1;
            background: #f5f5f5;
            overflow: hidden;
            transition: transform 0.3s ease;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 3rem;
        }

        .illustration-item:hover {
            transform: scale(1.05);
            z-index: 10;
        }

        /* 燈箱模式 */
        .lightbox {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.9);
            z-index: 2000;
            justify-content: center;
            align-items: center;
        }

        .lightbox.active {
            display: flex;
        }

        .lightbox-content {
            max-width: 90%;
            max-height: 90%;
            background: #f5f5f5;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 10rem;
            padding: 3rem;
        }

        .lightbox-close {
            position: absolute;
            top: 2rem;
            right: 2rem;
            color: white;
            font-size: 3rem;
            cursor: pointer;
            background: none;
            border: none;
            transition: opacity 0.3s ease;
        }

        .lightbox-close:hover {
            opacity: 0.7;
        }

        .lightbox-nav {
            position: absolute;
            top: 50%;
            transform: translateY(-50%);
            color: white;
            font-size: 3rem;
            cursor: pointer;
            background: none;
            border: none;
            padding: 1rem;
            transition: opacity 0.3s ease;
        }

        .lightbox-nav:hover {
            opacity: 0.7;
        }

        .lightbox-prev {
            left: 2rem;
        }

        .lightbox-next {
            right: 2rem;
        }

        /* LINE 產品卡片 */
        .line-products {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
            gap: 3rem;
            max-width: 1200px;
            margin: 0 auto;
        }

        .product-card {
            background: white;
            transition: transform 0.3s ease;
        }

        .product-card:hover {
            transform: translateY(-5px);
        }

        .product-image {
            width: 100%;
            height: 400px;
            background: #f5f5f5;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 5rem;
            margin-bottom: 1.5rem;
        }

        .product-content {
            padding: 0 1rem 2rem 1rem;
            text-align: center;
        }

        .product-content h3 {
            font-size: 1rem;
            color: #2c2c2c;
            font-weight: 400;
            margin-bottom: 2rem;
            letter-spacing: 0.5px;
        }

        .buy-button {
            display: inline-block;
            padding: 0.8rem 2rem;
            background: #2c2c2c;
            color: white;
            text-decoration: none;
            font-size: 0.85rem;
            font-weight: 400;
            letter-spacing: 1px;
            text-transform: uppercase;
            transition: all 0.3s ease;
            border: 1px solid #2c2c2c;
        }

        .buy-button:hover {
            background: white;
            color: #2c2c2c;
        }

        /* 頁尾 */
        footer {
            text-align: center;
            padding: 4rem 2rem;
            color: #999;
            font-size: 0.85rem;
            letter-spacing: 0.5px;
            border-top: 1px solid #e8e8e8;
            margin-top: 6rem;
        }

        /* 手機版選單按鈕 */
        .menu-toggle {
            display: none;
            position: fixed;
            top: 1.5rem;
            left: 1.5rem;
            z-index: 2000;
            background: #2c2c2c;
            color: white;
            border: none;
            padding: 0.8rem 1rem;
            cursor: pointer;
            font-size: 0.8rem;
            letter-spacing: 1px;
            text-transform: uppercase;
        }

        /* 響應式設計 */
        @media (max-width: 1024px) {
            .illustration-grid {
                grid-template-columns: repeat(2, 1fr);
            }
        }

        @media (max-width: 768px) {
            .sidebar {
                transform: translateX(-100%);
                transition: transform 0.3s ease;
            }

            .sidebar.active {
                transform: translateX(0);
            }

            .main-content {
                margin-left: 0;
            }

            .menu-toggle {
                display: block;
            }

            section {
                padding: 4rem 2rem;
                margin-top: 4rem;
            }

            section h2 {
                font-size: 1rem;
                margin-bottom: 3rem;
            }

            .illustration-grid {
                grid-template-columns: 1fr;
                gap: 0.5rem;
            }

            .line-products {
                grid-template-columns: 1fr;
                gap: 3rem;
            }

            .product-image {
                height: 350px;
            }

            .lightbox-content {
                font-size: 6rem;
                padding: 2rem;
            }

            .lightbox-close {
                top: 1rem;
                right: 1rem;
                font-size: 2rem;
            }

            .lightbox-nav {
                font-size: 2rem;
                padding: 0.5rem;
            }

            .lightbox-prev {
                left: 0.5rem;
            }

            .lightbox-next {
                right: 0.5rem;
            }
        }
    </style>
</head>
<body>
    <!-- 手機版選單按鈕 -->
    <button class="menu-toggle" onclick="toggleMenu()">MENU</button>

    <!-- 側邊欄 -->
    <aside class="sidebar" id="sidebar">
        <a class="logo" onclick="showPage('illustrations')">Peggy's illustration</a>
        <nav>
            <ul>
                <li><a onclick="showPage('illustrations')" class="nav-link active" data-page="illustrations">插畫作品</a></li>
                <li><a onclick="showPage('line-emoji')" class="nav-link" data-page="line-emoji">LINE 表情貼</a></li>
                <li><a onclick="showPage('line-stickers')" class="nav-link" data-page="line-stickers">LINE 貼圖</a></li>
                <li><a onclick="showPage('line-themes')" class="nav-link" data-page="line-themes">LINE 主題</a></li>
            </ul>
        </nav>
    </aside>

    <!-- 燈箱模式 -->
    <div class="lightbox" id="lightbox" onclick="closeLightbox(event)">
        <button class="lightbox-close" onclick="closeLightbox(event)">&times;</button>
        <button class="lightbox-nav lightbox-prev" onclick="navigateLightbox(-1, event)">&#8249;</button>
        <div class="lightbox-content" id="lightbox-content"></div>
        <button class="lightbox-nav lightbox-next" onclick="navigateLightbox(1, event)">&#8250;</button>
    </div>

    <!-- 主要內容區 -->
    <div class="main-content">
        <!-- 插畫作品頁面 -->
        <div class="page active" id="illustrations">
            <section>
                <h2>插畫作品</h2>
                <div class="illustration-grid">
                    <div class="illustration-item" onclick="openLightbox(0)">🎨</div>
                    <div class="illustration-item" onclick="openLightbox(1)">🖌️</div>
                    <div class="illustration-item" onclick="openLightbox(2)">✏️</div>
                    <div class="illustration-item" onclick="openLightbox(3)">🎭</div>
                    <div class="illustration-item" onclick="openLightbox(4)">🌸</div>
                    <div class="illustration-item" onclick="openLightbox(5)">🦋</div>
                    <div class="illustration-item" onclick="openLightbox(6)">🌈</div>
                    <div class="illustration-item" onclick="openLightbox(7)">⭐</div>
                    <div class="illustration-item" onclick="openLightbox(8)">🎪</div>
                </div>
            </section>
            <footer>
                <p>Copyright © 2024 Peggy's illustration. All rights reserved.</p>
            </footer>
        </div>

        <!-- LINE 表情貼頁面 -->
        <div class="page" id="line-emoji">
            <section>
                <h2>LINE 表情貼</h2>
                <div class="line-products">
                    <div class="product-card">
                        <div class="product-image">🤗</div>
                        <div class="product-content">
                            <h3>溫馨表情包</h3>
                            <a href="#" class="buy-button">立即購買</a>
                        </div>
                    </div>
                    <div class="product-card">
                        <div class="product-image">😎</div>
                        <div class="product-content">
                            <h3>酷炫表情包</h3>
                            <a href="#" class="buy-button">立即購買</a>
                        </div>
                    </div>
                    <div class="product-card">
                        <div class="product-image">🥰</div>
                        <div class="product-content">
                            <h3>戀愛表情包</h3>
                            <a href="#" class="buy-button">立即購買</a>
                        </div>
                    </div>
                </div>
            </section>
            <footer>
                <p>Copyright © 2024 Peggy's illustration. All rights reserved.</p>
            </footer>
        </div>

        <!-- LINE 貼圖頁面 -->
        <div class="page" id="line-stickers">
            <section>
                <h2>LINE 貼圖</h2>
                <div class="line-products">
                    <div class="product-card">
                        <div class="product-image">😊</div>
                        <div class="product-content">
                            <h3>可愛日常貼圖系列</h3>
                            <a href="#" class="buy-button">立即購買</a>
                        </div>
                    </div>
                    <div class="product-card">
                        <div class="product-image">🐱</div>
                        <div class="product-content">
                            <h3>貓咪表情貼圖</h3>
                            <a href="#" class="buy-button">立即購買</a>
                        </div>
                    </div>
                    <div class="product-card">
                        <div class="product-image">💕</div>
                        <div class="product-content">
                            <h3>愛心問候貼圖</h3>
                            <a href="#" class="buy-button">立即購買</a>
                        </div>
                    </div>
                    <div class="product-card">
                        <div class="product-image">🎉</div>
                        <div class="product-content">
                            <h3>節慶祝福貼圖</h3>
                            <a href="#" class="buy-button">立即購買</a>
                        </div>
                    </div>
                </div>
            </section>
            <footer>
                <p>Copyright © 2024 Peggy's illustration. All rights reserved.</p>
            </footer>
        </div>

        <!-- LINE 主題頁面 -->
        <div class="page" id="line-themes">
            <section>
                <h2>LINE 主題</h2>
                <div class="line-products">
                    <div class="product-card">
                        <div class="product-image">🌙</div>
                        <div class="product-content">
                            <h3>夢幻星空主題</h3>
                            <a href="#" class="buy-button">立即購買</a>
                        </div>
                    </div>
                    <div class="product-card">
                        <div class="product-image">🌺</div>
                        <div class="product-content">
                            <h3>清新花園主題</h3>
                            <a href="#" class="buy-button">立即購買</a>
                        </div>
                    </div>
                    <div class="product-card">
                        <div class="product-image">🌊</div>
                        <div class="product-content">
                            <h3>海洋風情主題</h3>
                            <a href="#" class="buy-button">立即購買</a>
                        </div>
                    </div>
                </div>
            </section>
            <footer>
                <p>Copyright © 2024 Peggy's illustration. All rights reserved.</p>
            </footer>
        </div>
    </div>

    <script>
        let currentLightboxIndex = 0;
        const illustrations = ['🎨', '🖌️', '✏️', '🎭', '🌸', '🦋', '🌈', '⭐', '🎪'];

        function showPage(pageId) {
            const pages = document.querySelectorAll('.page');
            pages.forEach(page => page.classList.remove('active'));
            document.getElementById(pageId).classList.add('active');
            
            const navLinks = document.querySelectorAll('.nav-link');
            navLinks.forEach(link => link.classList.remove('active'));
            document.querySelector(`[data-page="${pageId}"]`).classList.add('active');
            
            if (window.innerWidth <= 768) {
                closeMenu();
            }
            
            window.scrollTo(0, 0);
        }

        function openLightbox(index) {
            currentLightboxIndex = index;
            const lightbox = document.getElementById('lightbox');
            const content = document.getElementById('lightbox-content');
            content.innerHTML = illustrations[index];
            lightbox.classList.add('active');
            document.body.style.overflow = 'hidden';
        }

        function closeLightbox(event) {
            if (event.target.id === 'lightbox' || event.target.classList.contains('lightbox-close')) {
                const lightbox = document.getElementById('lightbox');
                lightbox.classList.remove('active');
                document.body.style.overflow = 'auto';
            }
        }

        function navigateLightbox(direction, event) {
            event.stopPropagation();
            currentLightboxIndex += direction;
            if (currentLightboxIndex < 0) {
                currentLightboxIndex = illustrations.length - 1;
            } else if (currentLightboxIndex >= illustrations.length) {
                currentLightboxIndex = 0;
            }
            const content = document.getElementById('lightbox-content');
            content.innerHTML = illustrations[currentLightboxIndex];
        }

        function toggleMenu() {
            const sidebar = document.getElementById('sidebar');
            sidebar.classList.toggle('active');
        }

        function closeMenu() {
            if (window.innerWidth <= 768) {
                const sidebar = document.getElementById('sidebar');
                sidebar.classList.remove('active');
            }
        }

        document.querySelector('.main-content').addEventListener('click', function() {
            if (window.innerWidth <= 768) {
                closeMenu();
            }
        });

        // ESC 鍵關閉燈箱
        document.addEventListener('keydown', function(e) {
            if (e.key === 'Escape') {
                const lightbox = document.getElementById('lightbox');
                if (lightbox.classList.contains('active')) {
                    lightbox.classList.remove('active');
                    document.body.style.overflow = 'auto';
                }
            }
        });
    </script>
</body>
</html>
